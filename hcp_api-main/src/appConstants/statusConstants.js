const notYetContacted = 'Not yet contacted';
const appointmentScheduled = 'Appointment scheduled';
const consenting = 'Consenting';
const enrolled = 'Enrolled';
const ineligible = 'Ineligible';
const droppedOut = 'Dropped out';
const notified = 'Notified';

module.exports = {
  notYetContacted,
  appointmentScheduled,
  consenting,
  enrolled,
  ineligible,
  droppedOut,
  notified,
  clinicalTrialAppointmentStatus: [
    notYetContacted,
    appointmentScheduled,
    consenting,
    enrolled,
    ineligible,
    droppedOut,
    notified,
  ],
};
